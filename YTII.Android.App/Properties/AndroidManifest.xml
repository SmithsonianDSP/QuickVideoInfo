<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="YTII.Android.App.YTII.Android.App" android:versionCode="1" android:versionName="1.0" android:installLocation="auto">
	<uses-sdk android:minSdkVersion="16" />
  <uses-permission android:name="android.permission.INTERNET" />
	<application android:label="YouTube Intent Interceptor" android:theme="@android:style/Theme.Translucent" android:excludeFromRecents="true">
    <activity
      android:name=".ui.MainActivity"
      android:excludeFromRecents="true"
      android:autoRemoveFromRecents="true"
      android:configChanges="orientation|screenSize"
      android:theme="@style/TranslucentActivity">
      <intent-filter>
        <action android:name="android.intent.action.SEND" />
        <category android:name="android.intent.category.DEFAULT" />
        <data android:mimeType="text/plain" />
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="http" android:host="*.youtube.com"/>
        <data android:scheme="https" android:host="*.youtube.com" />
      </intent-filter>
    </activity>

  </application>
</manifest>